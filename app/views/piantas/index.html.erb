<div class="container">
  <div class="admin-buttons">
    <%= link_to "Crea nuova pianta", new_pianta_path, class: "btn btn-success" %>
  </div>

  <div class="catalogo-content">
    <h1>Catalogo</h1>
    <ul>
      <% @piante.each do |pianta| %>
        <li><%= link_to pianta.name, pianta_path(pianta.id) %></li>
      <% end %>
    </ul>
  </div>
</div>
