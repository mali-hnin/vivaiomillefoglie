var id = <%= @line_item.plant_id %>;
<% if @line_item.save %>
  $(".howmany").html("<%= (cart_count + @temp_line_item.quantity).to_i %>");
  toastr.success(" <%= j (@line_item.plant.name) %> aggiunta al carrello.");
<%# for some frustratingly unknown reason, the following line doesnt work even tho it works in console. the code per se is right,
but for some reason i dont understand effing jquery doesnt get the id i think (vanilla js doesnt work either, btw) %>
  $("#cart-button-"+id).html("<i class='fas fa-check'></i>");
<% else %>
  alert("Errore interno: la pianta non è stata aggiunta al carrello. Ricarica la pagina e prova di nuovo.");
<% end %>
